machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - cd circleci && ./buildContainers.sh

deployment:
  hub:
    branch: [master]
    commands:
      - cd circleCi && ./pushContainers.sh
